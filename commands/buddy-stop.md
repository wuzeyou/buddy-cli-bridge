---
description: Stop the Hardware Buddy CLI bridge daemon and release the BLE connection.
disable-model-invocation: true
allowed-tools: Bash
---

```!
"${CLAUDE_PLUGIN_ROOT}/scripts/stop.sh"
```

Print the stop script's output above to the user verbatim. Do not add commentary.
